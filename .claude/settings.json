{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 scripts/check-evidence-gate-hook.py || python scripts/check-evidence-gate-hook.py"
          }
        ]
      }
    ]
  },
  "permissions": {
    "deny": [
      "Write(.env*)",
      "Edit(.env*)",
      "Write(**/*.pem)",
      "Edit(**/*.pem)",
      "Write(**/*secret*)",
      "Edit(**/*secret*)"
    ],
    "ask": [
      "Edit(package.json)",
      "Edit(build.gradle)",
      "Edit(settings.gradle)",
      "Edit(docker-compose*.yml)",
      "Bash(git commit*)",
      "Bash(git push*)"
    ]
  }
}
